# Pattern Generator


<!-- WARNING: THIS FILE WAS AUTOGENERATED! DO NOT EDIT! -->

The
[`PatternGenerator`](https://netzzwergeGithub.github.io/crossword-generator/pattern_generator.html#patterngenerator)
class takes a crossword slot pattern (like `'  x ch  p'`) and generates
all valid sub-patterns where words could be placed, sorted by constraint
count, length, and position.

## Usage

    from crossword_generator.pattern_generator import PatternGenerator

    # Create a pattern generator for a slot
    slot = '  x ch  p'
    pg = PatternGenerator(slot)

    # Generate all valid patterns
    patterns = pg.generate_patterns()

    # Returns list of (offset, pattern) tuples
    # Example: [(0, 'x ch'), (2, 'ch  p'), ...]

## How it works

The generator follows these rules: 1. Patterns must start at slot
beginning OR have space before (for `#` marker) 2. Patterns must end at
slot end OR have space after (for `#` marker)  
3. Minimum length of 2 characters 4. Must contain at least one non-space
character

Patterns are sorted by: - Constraint count (most constrained first) -
Length (longest first) - Position type (start/end before middle)

------------------------------------------------------------------------

<a
href="https://github.com/netzzwergeGithub/crossword-generator/blob/main/crossword_generator/pattern_generator.py#L12"
target="_blank" style="float:right; font-size:smaller">source</a>

### PatternGenerator

>  PatternGenerator (slot_pattern:str, min_word_length=2, placeholder=' ')

*Initialize self. See help(type(self)) for accurate signature.*

<table>
<colgroup>
<col style="width: 6%" />
<col style="width: 25%" />
<col style="width: 34%" />
<col style="width: 34%" />
</colgroup>
<thead>
<tr>
<th></th>
<th><strong>Type</strong></th>
<th><strong>Default</strong></th>
<th><strong>Details</strong></th>
</tr>
</thead>
<tbody>
<tr>
<td>slot_pattern</td>
<td>str</td>
<td></td>
<td>the input pattern to work with</td>
</tr>
<tr>
<td>min_word_length</td>
<td>int</td>
<td>2</td>
<td>minimal resulting word length using a a subpattern</td>
</tr>
<tr>
<td>placeholder</td>
<td>str</td>
<td></td>
<td>character in the pattern that can be used for any letter</td>
</tr>
</tbody>
</table>

``` python
# Test with slot variable
slot = '  x ch  p'
pg = PatternGenerator(slot)
result = pg.generate_patterns()
result_patterns = set([x[1] for x in result])

assert '  x ch  p' in result_patterns
assert '  x' in result_patterns
assert '  x ch' in result_patterns
assert '  x ch ' in result_patterns
assert ' x' in result_patterns
assert ' x ch' in result_patterns
assert ' x ch ' in result_patterns
assert ' x ch  p' in result_patterns
assert 'x ch' in result_patterns
assert 'x ch ' in result_patterns
assert 'x ch  p' in result_patterns
assert 'ch' in result_patterns
assert 'ch ' in result_patterns
assert 'ch  p' in result_patterns
assert ' p' in result_patterns

# Negative tests
pg1 = PatternGenerator('x', min_word_length=2)
result1 = set([x[1] for x in pg1.generate_patterns()])
assert len(result1) == 0

pg2 = PatternGenerator('', min_word_length=2)
result2 = set([x[1] for x in pg2.generate_patterns()])
assert len(result2) == 0

pg3 = PatternGenerator('    ', min_word_length=2)
result3 = set([x[1] for x in pg3.generate_patterns()])
assert all(' ' in p for p in result3)

pg4 = PatternGenerator('abc', min_word_length=2)
result4 = set([x[1] for x in pg4.generate_patterns()])
assert len(result4) == 1
```
